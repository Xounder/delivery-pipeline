DONE: Added autocomplete dropdown to SkillsTagsInput and CompanyFilters; created backend /jobs/suggestions endpoint; made search run on initial load (empty query); fixed search response format mismatch between backend and frontend types; consolidated both AGENTS.md files into one; fixed AutocompleteInput dropdown filter bug
WRONG: Did not load INDEX.md first; did not parallelize frontend/backend implementation; did not call QA reviewer skill; implemented without following pipeline workflow from AGENTS.md; AutocompleteInput dropdown only showed when input was empty (input.length === 0) blocking type-to-filter; did not run full validation commands (lint+tsc combined)
IMPROV: Always load .opencode/INDEX.md before starting implementation; split frontend and backend tasks into parallel agents; run QA after every implementation task; follow Direct Task Mode from AGENTS.md (assign agent → QA → STOP); use full validation commands from AGENTS.md (/validate:frontend, /validate:backend) not just individual tools
LEARN: .opencode/AGENTS.md was a superset of root AGENTS.md with updated test status and STOP hook; root file was stale; having two AGENTS.md files causes confusion; Autocomplete dropdown should only check focus + has matching items, not input length — filtering is done by the suggestions filter itself
NEXT: Run QA reviewer skill for both frontend and backend changes; verify both builds pass full validation; test the autocomplete type-to-filter behavior